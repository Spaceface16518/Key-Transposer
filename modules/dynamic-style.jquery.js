function setDynamicStyle() {
  $("*").css({
    "box-sizing": "border-box"
  });
  $("body").css({
    "font-family": "'Helvetica Neue', Helvetica, sans-serif",
    "font-size": "13px",
    "background-color": "rgb(84, 228, 168)",
    "text-align": "center"
  });
  // This is not all of the the css page, just enough to test it out
}
